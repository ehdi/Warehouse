server:
  port: 0

spring:
  profiles:
    active: test
  application:
    name: warehouse-product
  data:
    mongodb:
      host: localhost
      port: 0
      database: product

application:
  external:
    inventory: http://localhost:8181
